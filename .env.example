# 记账本 App 前端环境变量（Vite 构建时注入）
# ------------------------------------------------------------------
# 后端服务器地址：构建时固定进代码，安装到手机后用户不可更改。
#   - 本地开发 npm run dev：可不设置，默认 http://localhost:3000
#   - 安卓真机联调：填电脑局域网 IP，如 http://192.168.1.100:3000
#   - 正式发布：填你的服务器域名，如 https://api.example.com
# 修改后需重新构建（npm run build && npx cap sync）才生效。
# VITE_API_URL=http://localhost:3000
