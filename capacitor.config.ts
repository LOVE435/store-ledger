import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jzb.ledger',
  appName: '记账本',
  webDir: 'dist',
  android: {
    backgroundColor: '#f1f5f9',
  },
};

export default config;
